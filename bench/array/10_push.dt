#! bench: build n elements — push loop
#! n: 1000000
use std::io::print;
use std::array;

fun main() {
    var src = array::fill(1000000, 7);
    var xs: [int] = [];
    for _i in src {
        xs.push(7);
    }
    print("{xs.len()}");
}
