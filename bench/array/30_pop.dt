#! bench: drop n elements — pop loop
#! n: 1000000
use std::io::print;
use std::option::Option;
use std::array;

fun main() {
    var src = array::fill(1000000, 7);
    while var Option::Some(_v) = src.pop() {
    }
    print("{src.len()}");
}
