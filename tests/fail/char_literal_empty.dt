#! expect: empty character literal
# A character literal holds exactly one character, so there is no such thing as
# an empty one — unlike a string, whose emptiness is an ordinary length.
fun main() {
    var c = '';
}
