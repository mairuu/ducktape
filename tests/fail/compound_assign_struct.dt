#! expect: cannot apply '+' to a value of type '[int]': it does not implement 'Add'
# Same precondition, an aggregate instead of a tagged scalar: this used to
# reach OP_ADD and segfault the VM.
fun main() {
    var xs = [1, 2];
    xs += [3];
}
