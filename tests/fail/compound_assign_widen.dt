#! expect: type mismatch: expected 'Int' but got 'Float'
# `a op= b` is `a = a op b`, so the operator's *result* must fit the place:
# `Int + Float` is a Float, and a Float does not go back into an Int.
fun main() {
    var i = 1;
    i += 1.5;
}
