#! expect: 'dyn' needs a trait
# `dyn` names a trait and nothing else — a struct already has one
# representation, so there is no vtable to attach.

use std::io::print;

struct Sq { s: int }

fun use_it(x: dyn Sq) -> int {
    return 1;
}

fun main() {
    print(use_it(Sq { s: 2 }));
}
