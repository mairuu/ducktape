#! expect: field 'h' of struct 'Rect' is private in module
mod shape;

use shape::Rect;
use shape::square;

fun main() {
  var r = square(3);
  # w is pub, this would be fine; h is private, so reading it here is the error.
  var hidden: int = r.h;
}
