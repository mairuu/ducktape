#! expect: expected '!', which only diverging code produces, but got '()'
# A `loop` diverges only when nothing leaves it. One `break` gives it an exit,
# so it types `()` like a `while` and the promise is broken.
fun serve() -> ! {
    loop { break; }
}

fun main() {
    serve();
}
