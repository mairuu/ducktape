#! expect: circular module dependency
# the cycle is between two children: the root itself is not addressable, so a
# `use` can never close a loop through it.
mod a;
mod b;

use std::io::print;
use a::f;

fun main() {
  print(f());
}
