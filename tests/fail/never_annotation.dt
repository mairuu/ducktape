#! expect: expected 'Never', which only diverging code produces, but got 'Int'
# `Never` is nameable anywhere a type goes, and nowhere is a value of it
# producible: an annotated binding is checked like any other expectation.
fun main() {
    var x: Never = 5;
}
