#! expect: expected '!', which only diverging code produces, but got 'int'
# `!` is nameable anywhere a type goes, and nowhere is a value of it
# producible: an annotated binding is checked like any other expectation.
fun main() {
    var x: ! = 5;
}
