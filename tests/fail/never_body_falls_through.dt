#! expect: expected '!', which only diverging code produces, but got '()'
# A `-> !` body that runs off its end breaks the promise. Its Unit used to
# reach whatever the caller declared.
fun evil() -> ! { }

fun main() {
    var x: int = evil();
}
