#! expect: expected '!', which only diverging code produces, but got 'int'
# `return` in a `-> !` body is the same broken promise, one construct in.
fun evil() -> ! {
    return 3;
}

fun main() {
    var x: int = evil();
}
