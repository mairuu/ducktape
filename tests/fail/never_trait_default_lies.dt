#! expect: expected '!', which only diverging code produces, but got '()'
# The default body is checked once, at the trait, so no impl has to reach it.
trait Bail {
    fun stop(self) -> ! { }
}

fun main() { }
