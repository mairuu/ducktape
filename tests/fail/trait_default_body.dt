#! expect: type mismatch: expected 'int' but got 'string'
# default method bodies are checked once, against the abstract Self.
trait Named {
    fun name(self) -> string;

    fun id(self) -> int {
        return self.name();
    }
}

struct P {
    x: int,
}

impl Named for P {
    fun name(self) -> string {
        "p"
    }
}

fun main() {
    var p = P { x: 1 };
}
