#! expect: trait 'into' has no method named 'nope'
# The final segment must name a method the trait declares.
trait into<T> {
    fun into(self) -> T;
}

struct A { x: int }

fun main() {
    var a = A { x: 1 };
    var b = into::<A>::nope(a);
}
