#! expect: type 'Q' does not implement trait 'A'
# an explicitly written type argument must satisfy the parameter's bounds too.
trait A {
    fun a(self) -> int;
}

struct P {
    x: int,
}

struct Q {
    y: int,
}

impl A for P {
    fun a(self) -> int {
        self.x
    }
}

fun need_a<T: A>(t: T) -> int {
    0
}

fun main() {
    var q = Q { y: 1 };
    var r = need_a::<Q>(q);
}
