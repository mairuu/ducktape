#! expect: assertion failed
# `assert` has no way to quote its own argument — no macros, and the callee
# receives a `bool` — so the message is this fixed string and nothing more.
use std::assert::assert;

fun main() {
    assert(2 + 2 == 5);
}
