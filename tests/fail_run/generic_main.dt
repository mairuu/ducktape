#! expect: 'main' must not be generic
# a generic definition has no body of its own — only the copies its call sites
# ask for — and nothing calls the entry point.
use std::io::print;

fun main<T>() {
  print(1);
}
