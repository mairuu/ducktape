# array literals, indexing, [T] type annotations, iteration
fun sum(xs: [Int]) -> Int {
    var total = 0;
    for x in xs {
        total = total + x;
    }
    total
}

fun main() {
    var xs = [1, 2, 3];
    var _first = xs[0] + 1;
    var _s = sum(xs);
    var _annotated: [Int] = [4, 5, 6];
    var nested = [[1], [2, 3]];
    var _inner = nested[1][0];
    var _strs = ["a", "b"];
}
