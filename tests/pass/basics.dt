# struct/trait/impl + method calls + return statements + unit
struct Point {
    x: int,
    y: int,
}

impl Point {
    fun new() -> Self {
        Point { x: 0, y: 0 }
    }
}

trait Done {
    fun do(self);
}

impl Done for Point {
    fun do(self) {}
}

fun early(n: int) -> int {
    if n > 0 {
        return n;
    }
    0
}

fun tail_return(n: int) -> int {
    return n * 2;
}

fun main() {
    var p = Point::new();
    p.do();
    var _u: () = ();
    var _x = early(3) + tail_return(2);
}
