# imported by main.dt

pub struct Point {
  pub x: int,
  pub y: int,
}

pub fun origin() -> Point {
  return Point { x: 0, y: 0 };
}

# never imported: private items are invisible across the module boundary,
# but must not stop the module from being used.
fun secret() -> int {
  return 42;
}
