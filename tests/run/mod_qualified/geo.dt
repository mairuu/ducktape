pub struct Point {
  x: int,
  y: int,
}

impl Point {
  fun new(x: int, y: int) -> Point {
    return Point { x: x, y: y };
  }
  fun manhattan(self) -> int {
    return self.x + self.y;
  }
}

pub enum Quadrant {
  First,
  Other,
}

pub fun classify(p: Point) -> Quadrant {
  if p.x > 0 {
    if p.y > 0 {
      return Quadrant::First;
    }
  }
  return Quadrant::Other;
}
