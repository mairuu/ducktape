# a bare unit struct is a value under its own name, in an expression and in a
# pattern — `Marker` needs no `{}` suffix.
#> Marker
#> true
#> 7
#> [Marker, Marker]
#> Wrap
#> matched Marker

use std::io::print;

struct Marker;
struct Wrap<T>;

fun take(_m: Marker) -> int => 7

fun main() {
  var m = Marker;
  print(m);
  print(m == Marker {});
  print(take(Marker));
  print([Marker, Marker]);

  # a generic unit struct still takes its type argument from the annotation
  var w: Wrap<int> = Wrap;
  print(w);

  match m {
    Marker => print("matched Marker"),
  }
}
